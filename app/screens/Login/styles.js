import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    flex: 1,
  },
  form: {
    alignItems: 'center',
    marginTop: 50,
  },
  image: {
    height: 330,
    width: 330,
    alignSelf: 'center',
  },
});

export default styles;
